# legacy/

Retired docs kept for reference. Their live, forward-looking content has been folded into
`roadmap.md` (STATUS, BACKLOG, Done log) and `memory.md` (decisions and learned rules) at the repo
root. Nothing here is current.

- `roadmap-v0.6.md` is the closed-out v0.6 "Trust Release" cycle roadmap (was `docs/roadmap.md`). It
  carries the full cycle rationale, the locked decisions, the metric-correctness audit lineage, and
  the exact-VEI narrative through its close-out in 0.6.2.
- `STATUS-2026-06-10.md` is a v0.6.1-era status snapshot (was the root `STATUS.md`). It is stale, it
  lists the exact-VEI item as parked, but that was closed in 0.6.2.
- `handoff-2026-06-10.md` is the matching session handoff (was the root `handoff.md`).
